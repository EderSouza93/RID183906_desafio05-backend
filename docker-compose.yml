version: '3.8'

services:
  db:
    image: postgres:latest
    container_name: desafio5_RID183906
    environment:
      POSTGRES_DB: desafio5_RID183906
      POSTGRES_USER: developer
      POSTGRES_PASSWORD: admin
    ports:
    - '5433:5432'
